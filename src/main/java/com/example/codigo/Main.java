package com.example.codigo;
import javax.sound.sampled.*;

/**
 * Este es el metodo main que llama la primer interfaz, siendo el inicio del codigo, iniciando el thread principal.
 * @authors Emmanuel Esquivel Chavarria & Andres Madrigal Vega
 */
public class Main extends LogInApplication{
    /**
     * Metodo que carga todos los args, metodo main que corre el programa en cuestion.
     * @param args array que recibe datos de la command line .
     */
    static public void main(String[] args) {launch(args);}

}
